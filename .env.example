# Slipstream needs NO credentials for research. Every endpoint used by
# scripts/probe_polymarket.py and the persistence test is public.
#
# These are placeholders for a LATER phase (execution), and are listed only
# so nobody invents their own names. Do not fill them in yet.

# POLYGON_PRIVATE_KEY=      # NEVER commit. Execution phase only.
# POLYMARKET_API_KEY=       # derived from the key above via py-clob-client
# POLYMARKET_API_SECRET=
# POLYMARKET_API_PASSPHRASE=
